<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory
        android:key="mapbox_category"
        android:title="Mapbox">
        <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
        <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
        <ListPreference
            android:defaultValue="mapbox://styles/mapbox/dark-v9"
            android:entries="@array/pref_mapbox_style_titles"
            android:entryValues="@array/pref_mapbox_style_values"
            android:key="@string/pref_key_mapbox_style"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="@string/pref_title_mapbox_style" />
    </PreferenceCategory>

</PreferenceScreen>
